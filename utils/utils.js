module.exports.add = (a, b) => a+ b + 4; //<- Putting on one line means it's implicitly returned
// ^ Add a number at the end to make it fail the test (i.e., + 22)
module.exports.square = (x) => x * x;
// ^ Add a number at the end to make it fail the test (i.e., + 22)
